<!--index.wxml-->
<view class="index-box">
  <view class="weather-box" wx:for="{{ lists }}" wx:key="id" data-name="{{ item.name }}" bindtap="jump">
    <image class="weather" src="{{ item.src }}"></image>
    <text>{{ item.name }}</text>
  </view>
</view>